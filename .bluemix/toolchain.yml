---
name: "All In Toolchain"
description: "This toolchain will be used in Functional System Test.\n Click CREATE to start."
version: 0.1

# Github repos
sample-repo:
  service_id: githubpublic
  
# Pipelines
sample-build:
  service_id: pipeline
  parameters:
    name: SetUpPipeline
    ui-pipeline: true
    stages:
    - name: all-in-stage
      inputs:
      - url: https://github.com/TinaLindhqvist/AlmostEmpty.git
        type: git
        branch: master
      jobs:
      - name: Build
        type: builder    
